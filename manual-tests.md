# Manual Test Cases — REM Waste Booking Flow

## Test Coverage Summary
- **Total Cases:** 40
- **Positive Tests:** 20
- **Negative Tests:** 10
- **Edge Cases:** 6
- **API Failure Tests:** 4
- **State Transition Tests:** 4

---

## Test Cases

| ID | Title | Preconditions | Steps | Expected Result | Type |
|----|-------|---------------|-------|-----------------|------|
| TC-001 | Valid postcode returns multiple addresses | App loaded at /booking | 1. Enter "SW1A 1AA" in postcode input<br>2. Click "Find address" button<br>3. Observe address dropdown | Address dropdown appears with 12+ addresses listed. All addresses are real-sounding London addresses. Next button remains disabled until address selected. | Positive |
| TC-002 | Valid postcode with no addresses returns empty state | App loaded at /booking | 1. Enter "EC1A 1BB" in postcode input<br>2. Click "Find address" button<br>3. Observe result | Friendly message displayed: "No addresses found". "Enter address manually" link appears. No error styling shown. | Positive |
| TC-003 | Invalid postcode format shows validation error | App loaded at /booking | 1. Enter "INVALID" in postcode input<br>2. Click "Find address" button<br>3. Observe error message | Error message displayed: "Invalid postcode format". Button remains enabled for retry. Input shows error border styling. | Negative |
| TC-004 | Empty postcode disables find button | App loaded at /booking | 1. Leave postcode input empty<br>2. Observe "Find address" button state | "Find address" button is disabled. No error message shown. | Negative |
| TC-005 | Latency postcode shows persistent loading state | App loaded at /booking | 1. Enter "M1 1AE" in postcode input<br>2. Click "Find address" button<br>3. Observe loading behavior for 1500ms | Button shows spinner inside. Button is disabled during load. Input remains visible and unchanged. After 1500ms, addresses appear. | State Transition |
| TC-006 | Error postcode shows retry button on first attempt | App loaded at /booking | 1. Enter "BS1 4DJ" in postcode input<br>2. Click "Find address" button<br>3. Observe error state | Error message displayed with exact API error text. "Try again" button appears. Original input preserved. | API Failure |
| TC-007 | Retry on error postcode succeeds on second attempt | TC-006 completed (error state visible) | 1. Click "Try again" button<br>2. Observe result | Loading state shown briefly. Success: addresses appear in dropdown. Error message clears. | API Failure |
| TC-008 | Manual address entry accepted when no results | TC-002 completed (empty state) | 1. Click "Enter address manually" link<br>2. Type "123 Custom Street, London" in manual input<br>3. Click "Next" | Manual address input appears below. Next button enables when manual address has content. Proceeds to Step 2 with manual address stored. | Edge Case |
| TC-009 | Postcode auto-formats to uppercase | App loaded at /booking | 1. Type "sw1a 1aa" in lowercase in postcode input<br>2. Observe input value | Input value transforms to "SW1A 1AA" as user types. Formatting happens on each keystroke. | Positive |
| TC-010 | Address selection enables Next button | TC-001 completed (addresses loaded) | 1. Select first address from dropdown<br>2. Observe Next button state | Next button changes from disabled to enabled. Button shows no loading state. | Positive |
| TC-011 | Select general waste with no branching | Step 2 active | 1. Click "General Waste" card<br>2. Observe UI state | Card shows selected state (highlighted border, checkmark). Plasterboard options remain hidden. Next button enabled. | Positive |
| TC-012 | Select heavy waste with no branching | Step 2 active | 1. Click "Heavy Waste" card<br>2. Observe UI state | Card shows selected state. Plasterboard options remain hidden. Next button enabled. Helper text visible: "Includes soil, concrete, rubble, and similar dense materials." | Positive |
| TC-013 | Select plasterboard reveals branching options | Step 2 active | 1. Click "Plasterboard" card<br>2. Observe UI transition | Card shows selected state. Plasterboard sub-options smoothly reveal below using max-height transition. Helper text visible: "Plasterboard must be separated or declared. Choose how you will handle it below." Next button remains disabled. | State Transition |
| TC-014 | Plasterboard selected without sub-option blocks Next | TC-013 completed (plasterboard selected, options visible) | 1. Do not select any plasterboard sub-option<br>2. Attempt to click Next button | Next button remains disabled. No error message shown. Button has aria-disabled="true". | Negative |
| TC-015 | Change from plasterboard to general clears sub-option | TC-013 completed, then plasterboard sub-option selected | 1. Select "I will bag it separately" plasterboard option<br>2. Click "General Waste" card<br>3. Observe state | General Waste card becomes selected. Plasterboard options smoothly hide. Plasterboard sub-option selection is cleared from state. Next button enabled. | State Transition |
| TC-016 | Select "I will bag it separately" plasterboard option | TC-013 completed (plasterboard selected) | 1. Click "I will bag it separately" card<br>2. Observe state | Sub-option card shows selected state. Next button becomes enabled. Selection stored in state. | Positive |
| TC-017 | Select "It will be mixed with other waste" plasterboard option | TC-013 completed (plasterboard selected) | 1. Click "It will be mixed with other waste" card<br>2. Observe state | Sub-option card shows selected state. Next button becomes enabled. Selection stored in state. | Positive |
| TC-018 | Select "I need a separate collection" plasterboard option | TC-013 completed (plasterboard selected) | 1. Click "I need a separate collection" card<br>2. Observe state | Sub-option card shows selected state. Next button becomes enabled. Selection stored in state. | Positive |
| TC-019 | Load skips for general waste shows all enabled | Step 3 active, wasteType=general | 1. Observe skip grid on component mount<br>2. Wait for API response | 8 skip cards displayed in responsive grid (2 cols mobile, 3-4 desktop). All skips show enabled state (no opacity reduction). No "Not available" labels visible. | Positive |
| TC-020 | Load skips for heavy waste shows disabled skips | Step 3 active, wasteType=heavy | 1. Observe skip grid on component mount<br>2. Wait for API response<br>3. Count disabled skips | 8 skip cards displayed. At least 2 skips show disabled state: reduced opacity, "Not available for heavy waste" label visible. Disabled skips remain in DOM and visible in grid. | Positive |
| TC-021 | Select enabled skip highlights card and enables Next | TC-019 or TC-020 completed (skips loaded) | 1. Click an enabled skip card<br>2. Observe card state and Next button | Selected skip card shows highlighted border and checkmark badge in top-right. Next button changes from disabled to enabled. Price shown in sticky footer (mobile). | Positive |
| TC-022 | Attempt to select disabled skip has no effect | TC-020 completed (heavy waste, disabled skips visible) | 1. Click a disabled skip card<br>2. Observe result | No selection state applied to disabled card. No checkmark appears. Next button remains disabled if no other skip selected. Card shows cursor-not-allowed on hover. | Negative |
| TC-023 | No skip selected keeps Next button disabled | TC-019 completed (skips loaded, none selected) | 1. Observe Next button state without selecting any skip | Next button is disabled. Button has aria-disabled="true" attribute. No error message shown. | Negative |
| TC-024 | API failure on skip load shows error state | Step 3 active, API endpoint returns 500 | 1. Trigger API failure (mock or network error)<br>2. Observe error handling | Error message displayed: exact API error text. Skip grid not shown. Back button remains enabled. No Next button visible. | API Failure |
| TC-025 | Skip grid renders correctly on mobile 375px | TC-019 completed (skips loaded) | 1. Resize browser to 375px width<br>2. Observe skip grid layout | Grid shows 2 columns. All 8 cards visible and scrollable. Cards maintain readable size. Sticky price footer appears at bottom. Touch targets minimum 44px. | Edge Case |
| TC-026 | Skip grid renders correctly on desktop 1440px | TC-019 completed (skips loaded) | 1. Resize browser to 1440px width<br>2. Observe skip grid layout | Grid shows 3-4 columns. All 8 cards visible without scroll. Cards evenly spaced. Price footer not sticky on desktop. | Positive |
| TC-027 | Skeleton loader shown during skip API call | Step 3 component mounting | 1. Observe UI immediately on Step 3 load<br>2. Before API response completes | 8 placeholder cards shown with pulse animation. Grid layout matches final layout. No flash of empty state. | Positive |
| TC-028 | Disabled skip shows info icon and tooltip | TC-020 completed (disabled skips visible) | 1. Locate a disabled skip card<br>2. Observe disabled state styling | Card shows reduced opacity. Small info icon visible next to "Not available for heavy waste" text. Icon has aria-hidden="true". Label has descriptive aria-label. | Positive |
| TC-029 | Review step shows all booking details correctly | Step 4 active, all previous steps completed | 1. Observe review summary sections<br>2. Verify each detail | Postcode displayed correctly. Selected address shown (or manual address). Waste type label shown ("General Waste", "Heavy Waste", or "Plasterboard"). Plasterboard sub-option shown if applicable. Skip size displayed. Price shown in large, bold text. | Positive |
| TC-030 | Confirm booking shows success state with booking ID | TC-029 completed (review visible) | 1. Click "Confirm Booking" button<br>2. Wait for API response<br>3. Observe success state | Button immediately disabled and shows spinner. Success confirmation screen appears. Booking ID displayed matching pattern /^BK-/. Success icon (checkmark in green circle) shown. "Start a new booking" button visible. | Positive |
| TC-031 | Double-click confirm only fires one API call | TC-029 completed (review visible) | 1. Rapidly double-click "Confirm Booking" button<br>2. Monitor network tab for API calls | Only one POST request to /api/booking/confirm fires. Button disabled immediately after first click. No duplicate booking created. | Edge Case |
| TC-032 | API error on confirm shows inline error | TC-029 completed, API returns 400 or 500 | 1. Click "Confirm Booking" button<br>2. API returns error response<br>3. Observe error handling | Inline error message appears below button. Error shows exact API error text. Review form remains visible (not cleared). Button re-enables for retry. Booking ID not shown. | API Failure |
| TC-033 | Back from review preserves previous step state | Step 4 active, skip selected in Step 3 | 1. Click "Back" button<br>2. Observe Step 3 state | Returns to Step 3. Previously selected skip still shows selected state. All form state preserved (postcode, address, waste type). Step indicator updates to show Step 3 active. | State Transition |
| TC-034 | Price in review matches selected skip price | TC-029 completed (review visible) | 1. Note skip price from Step 3<br>2. Navigate to Step 4<br>3. Compare prices | Price shown in review exactly matches skip price from Step 3. No additional fees or modifications. Currency symbol (£) displayed. | Positive |
| TC-035 | Back navigation from any step preserves answers | Any step active with data entered | 1. Complete Step 1 (postcode + address)<br>2. Complete Step 2 (waste type)<br>3. Click Back to Step 2<br>4. Click Back to Step 1<br>5. Observe preserved state | Step 1: postcode and selected address still populated. Step 2: waste type selection preserved. Plasterboard sub-option preserved if applicable. All inputs remain filled. | Positive |
| TC-036 | Step indicator reflects current step accurately | Any step active | 1. Navigate through all 4 steps forward<br>2. Navigate backward<br>3. Observe step indicator at each step | Current step highlighted in indicator. Completed steps show checkmark. Future steps show default state. aria-current="step" on active step. Labels match step content. | Positive |
| TC-037 | Form usable on 375px mobile viewport | App loaded at /booking, viewport 375px | 1. Complete entire booking flow on 375px viewport<br>2. Test all interactions | All text readable without zoom. All buttons tappable (min 44px touch target). No horizontal scroll. Inputs full-width and accessible. Skip cards in 2-col grid. Sticky footer doesn't obscure content. | Edge Case |
| TC-038 | Keyboard navigation through entire flow | App loaded at /booking | 1. Use only Tab, Enter, Space, Arrow keys<br>2. Navigate through all 4 steps<br>3. Complete booking | All interactive elements reachable via Tab. Enter/Space activate buttons and select options. Focus visible on all elements. Skip cards navigable with arrow keys in radiogroup. No keyboard traps. | Edge Case |
| TC-039 | Plasterboard option appears in review when selected | Step 2: plasterboard + sub-option selected, Step 4 active | 1. Select Plasterboard waste type<br>2. Select "I will bag it separately"<br>3. Navigate to Step 4<br>4. Observe review details | Waste type shows "Plasterboard". Sub-option text shown below: "I will bag it separately". Both labels use plain English (not IDs). | Positive |
| TC-040 | Manual address appears in review instead of dropdown address | Step 1: manual address entered, Step 4 active | 1. Enter postcode "EC1A 1BB" (no results)<br>2. Enter manual address "123 Custom Street"<br>3. Complete Steps 2-3<br>4. Observe review | Review shows postcode "EC1A 1BB". Address line shows "123 Custom Street" (manual entry). No dropdown address shown. | Positive |
